[BAI TAP] Mô tả thuật toán tính điểm trung bình
BEGIN
	INPUT
	a= điểm toán
	b = điểm lý
	c= điểm hóa 
	AVERAGE = ( a+b+c)/3
	DISPLAY = AVERAGE
END

[BÀI TẬP] Quy đổi tiền tệ
BEGIN
	INPUT  usd
           vnd=usd*23000
           DISPLAY = vnd
END

[BÀI TẬP] Mô tả thuật toán tìm giá trị lớn nhất trong 3 chữ số
BEGIN
	INPUT A,B,C
	IF A MAX (A,B,C)THEN
		DISPLAY "A lớn nhất"
	ELSE IF B MAX(A,B,C)THEN
		DISPLAY "B lớn nhất"
	ELSE 
		DISPLAY "C lớn nhất"
END


 [BÀI TẬP] Mô tả thuật toán cấu trúc có điều kiện
BEGIN
INPUT N
IF N >= 75 THEN
 	DISPLAY "loại A"
 ELSE IF n >= 60 THEN
	DISPLAY " Loại B"	
ELSEIF n>=45 THEN
	DISPLAY "Loại C"
ELSEIF n>=35 THEN
 	DISPLAY "Loại D"
ELSE 
	DISPLAY "Loại E"
END



[BÀI TẬP]Thuật toán tìm giá trị lớn nhất của 1 dãy số nguyên

BEGIN
	INPUT a1,a2,a3...an
	INPUT N
	MAX = a1, i=2
	






[THỰC HÀNH] Thuật toán game đoán số
BEGIN
	INPUT N là số được sinh số ngẫu nhiên trong khoảng 0-9
	INPUT M
	IF N+M 
		DISPLAY " Dự đoán đúng" THEN
	ALSE 
		DISPLAY " Dự đoán sai"
END

[THỰC HÀNH] Thuật toán chuyển đổi nhiệt độ
BEGIN
	INPUT	C
	F= (9*C)/5+32
	DISPLAY F
END


